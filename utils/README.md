# Utils

Shared error handling and formatting helpers used across the workspace.

## Key exports
- `MyError`, `Result<T>` — unified error handling (`error.rs`)
- `format::*` — `date`, `number`, `percent`, `phone`, `text` formatters
- `classes!` macro — joins non-empty class strings with spaces
